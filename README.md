# reactnative_guess_a_number
Learning to build mobile apps with React Native. This app will take a user input of any number and then adjust min/max variables to narrow down on guessing the user's number.
